Launch a t2.micro EC2 instance named "vibeventure-app" using Amazon Linux 2023, in the same VPC as my RDS instance "vibeventure-db" (or "vibeventure" - check my RDS instances first). Create a new key pair called "vibeventure-key" and save the .pem file. Create a security group "vibeventure-app-sg" allowing inbound SSH (22) from my current IP, inbound TCP 5000 from anywhere, and inbound HTTP 80 from anywhere. Launch the instance and give me its public IP when ready.